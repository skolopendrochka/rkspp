spring:
  application:
    name: chechulina-processor
  datasource:
    url: jdbc:postgresql://localhost:5432/examdb
    username: postgres
    password: skolopendrochka
    driver-class-name: org.postgresql.Driver
    hikari:
      pool-name: PostgresHikariPool
  jpa:
    database: POSTGRESQL
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect
    database-platform: org.hibernate.dialect.PostgreSQLDialect
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest

server:
  port: 8082

springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html

clickhouse:
  url: jdbc:clickhouse://ehqkmwf7wz.eu-central-1.aws.clickhouse.cloud:8443/default?ssl=true
  username: default
  password: _NNvyvjc9ujDF

logging:
  level:
    ru.rksp: DEBUG
    org.hibernate.SQL: DEBUG